/*
      Libraries: Dart allows you to organize your code into libraries, which are collections 
      of related code. This can help you to manage your code and make it more modular and reusable.
*/

// Import the math library
import 'dart:math';

void main() {
  // Use the sqrt function from the math library
  var result = sqrt(25);

  // Print the result
  print(result);

  var power = pow(2, 2);
  print(power);
}
